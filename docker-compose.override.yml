services:
  api:
    build:
      target: builder
    # builder image includes nodemon; rebuild after changing target: docker compose up -d --build
    command: npx nodemon --watch src src/index.js
    environment:
      NODE_ENV: development
    volumes:
      - ./src:/app/src

  postgres:
    ports:
      - "5432:5432"
