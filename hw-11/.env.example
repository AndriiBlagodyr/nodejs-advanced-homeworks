# PostgreSQL URL without a password; the password is read from DB_PASSWORD_FILE.
DB_URL=postgresql://app@localhost:5432/marketplace

# HTTP port exposed by the API.
PORT=3000

# Runtime mode: development, production, or test.
NODE_ENV=development

# Path to the file containing the PostgreSQL password.
DB_PASSWORD_FILE=secrets/db_password
