# Dev credentials also live in docker-compose.yml. Runtime values come from
# Infisical via scripts/with-secrets.sh, or from the process environment when
# SKIP_VAULT=1 (CI / grader).
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=app
DB_PASSWORD=marketplace_dev_password
DB_NAME=marketplace
